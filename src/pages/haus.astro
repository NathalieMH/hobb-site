---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";
const base = import.meta.env.BASE_URL;

const allApts  = await getCollection("apartments");
const allRooms = await getCollection("rooms");
const aptCount  = allApts.length;
const roomCount = allRooms.length;
---

<Layout title="Villa Theo — Das Haus">

  <!-- Hero -->
  <section class="haus-hero">
    <img
      src={`${base}first-image.png`}
      alt="Villa Theo, Berlin Steglitz-Dahlem"
      loading="eager"
      data-always-fade
    />
  </section>

  <!-- Middle: text + facts left | staircase right -->
  <section class="haus-middle">
    <div class="middle-grid">

      <!-- LEFT: title text + facts pushed to bottom -->
      <div class="middle-left">
        <div class="text-anchor">
          <h2 class="middle-title">
            EINE HISTORISCHE<br> STADTVILLA
          </h2>
          <p class="middle-body">
            Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
          </p>
        </div>

        <div class="haus-facts">
          <h3 class="facts-heading" data-de>Ein Haus mit Geschichte</h3>
          <h3 class="facts-heading" data-en>A House with History</h3>
          <dl>
            <dt><span data-de>Baujahr</span><span data-en>Built</span></dt>
            <dd>1920er Jahre</dd>
            <dt><span data-de>Architekt</span><span data-en>Architect</span></dt>
            <dd>
              <a class="wiki-link" href="https://de.wikipedia.org/wiki/Leo_Nachtlicht" target="_blank" rel="noopener">
                Leo Nachtlicht
              </a>
            </dd>
            <dt><span data-de>Wohnungen</span><span data-en>Apartments</span></dt>
            <dd>{aptCount}</dd>
            <dt><span data-de>Zimmer gesamt</span><span data-en>Total rooms</span></dt>
            <dd>{roomCount}</dd>
            <dt><span data-de>Lage</span><span data-en>Location</span></dt>
            <dd>Berlin Steglitz · Dahlem</dd>
          </dl>
        </div>
      </div>

      <!-- RIGHT: staircase image -->
      <div class="middle-img">
        <img
          src={`${base}das-haus/staircase.jpg`}
          alt=""
          loading="lazy"
          data-always-fade
        />
      </div>

    </div>
  </section>

  <!-- Bottom: two images -->
  <section class="haus-bottom">
    <div class="img-pair">
      <img src={`${base}das-haus/vonoben.png`} alt="" loading="lazy" data-always-fade />
      <img src={`${base}das-haus/houseaesthetic.jpg`} alt="" loading="lazy" data-always-fade />
    </div>
  </section>

</Layout>

<style>

/* ── Shared horizontal padding ── */
.haus-hero,
.haus-middle,
.haus-bottom {
  padding-inline: clamp(2rem, 8vw, 5rem);
}

/* ── Hero ── */
.haus-hero {
  margin: 0;
  font-size: 0;
  line-height: 0;
  overflow: hidden;
  background: #fff;
}
.haus-hero img {
  display: block;
  width: 100%;
  height: clamp(320px, 52vw, 720px);
  object-fit: cover;
  object-position: center 35%;
}
@media (max-width: 600px) {
  .haus-hero { padding-inline: 0; }
  .haus-hero img { height: 56vw; }
}

/* ── Middle section ── */
.haus-middle {
  padding-top: clamp(2rem, 4vw, 3.5rem);
}

.middle-grid {
  display: grid;
  grid-template-columns: 2fr 3fr;
  align-items: stretch;
  min-height: clamp(480px, 60vw, 780px);
}

/* Left column: title at top, facts at bottom */
.middle-left {
  display: flex;
  flex-direction: column;
  padding-right: clamp(1.5rem, 3vw, 2.5rem);
}

.text-anchor {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.middle-title {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-size: clamp(1.8rem, 3vw, 2.5rem);
  font-weight: 300;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  line-height: 1.05;
  margin: 0;
  color: var(--ink);
}

.middle-body {
  font-size: 0.95rem;
  line-height: 1.7;
  color: var(--muted);
  margin: 0;
}

/* Facts — pushed to bottom of left column */
.haus-facts {
  margin-top: auto;
}

.facts-heading {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-size: clamp(1rem, 1.8vw, 1.3rem);
  font-weight: 300;
  letter-spacing: 0.04em;
  color: var(--ink);
  margin: 0 0 0.75rem;
}

.haus-facts dl {
  display: grid;
  grid-template-columns: max-content 1fr;
  gap: 0.5rem 1.5rem;
  align-items: baseline;
}
.haus-facts dt {
  font-size: 0.7rem;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--muted);
  padding-top: 0.1rem;
}
.haus-facts dd {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-size: 1.05rem;
  margin: 0;
}

.wiki-link {
  color: inherit;
  text-decoration: underline;
  text-decoration-color: var(--gold);
  text-underline-offset: 3px;
}
.wiki-link:hover { color: var(--gold); }

/* Staircase image — fills right column height */
.middle-img {
  overflow: hidden;
}
.middle-img img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

/* ── Bottom two images ── */
.haus-bottom {
  margin-top: clamp(2rem, 4vw, 3.5rem);
}
.img-pair {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
}
.img-pair img {
  display: block;
  width: 100%;
  height: clamp(220px, 28vw, 420px);
  object-fit: cover;
}

/* ── Responsive ── */
@media (max-width: 900px) {
  .middle-grid {
    grid-template-columns: 1fr;
    min-height: auto;
  }
  .middle-img { min-height: 50vw; order: -1; }
  .middle-img img { height: 100%; min-height: 50vw; }
  .img-pair { grid-template-columns: 1fr; }
  .img-pair img { height: 56vw; }
}
@media (max-width: 600px) {
  .haus-bottom { padding-inline: 0; }
}

</style>
